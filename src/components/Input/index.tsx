import Input, { Props, HandlersProps } from './Input'
// import { defaultProps, compose, withHandlers } from 'recompose';


// export default compose<Props & HandlersProps, Props & HandlersProps>(
// )(Input)
export default Input
export * from './Input'
